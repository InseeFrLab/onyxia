import { Props } from '@storybook/addon-docs/blocks';
import * as stories from './myservices.stories.js';
import ListeCartes from 'js/components/commons/service-liste/liste-cartes';

<Meta title={stories.default.title} component={stories.default.component} />

# My services

This component displays a list of cards for each service the user has

<Props of={ListeCartes} />

If user has no services :

<Story name="NoServices" decorators={stories.default.decorators}>
	{stories.NoServices()}
</Story>

When the user has a single service :

<Story name="SingleService" decorators={stories.default.decorators}>
	{stories.SingleService()}
</Story>
