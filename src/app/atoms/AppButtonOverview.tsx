import React from "react";
import { AppButton } from "./AppButton";

export function AppButtonOverview() {

    return (
        <div>
            <AppButton color="primary">Primary button</AppButton>
            <AppButton color="secondary">Secondary button</AppButton>
        </div>
    );

}