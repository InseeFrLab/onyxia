import { Props, Story } from '@storybook/addon-docs/blocks';
import * as stories from './formation-card.stories';
import FormationCard from './component';

<Meta title={stories.default.title} component={stories.default.component} />

# Formation card

If formation has not child formation neither deployment:

<Story name="none" decorators={stories.default.decorators}>
	{stories.None()}
</Story>

If formation has child formation but not deployment:

<Story name="formation" decorators={stories.default.decorators}>
	{stories.Formation()}
</Story>

If formation has not child formation but deployment:

<Story name="deployment" decorators={stories.default.decorators}>
	{stories.Deployment()}
</Story>

If formation has child formation and deployment:

<Story name="both" decorators={stories.default.decorators}>
	{stories.Both()}
</Story>
