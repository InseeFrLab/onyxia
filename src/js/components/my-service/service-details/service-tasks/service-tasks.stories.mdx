import { Props, Story } from '@storybook/addon-docs/blocks';
import * as stories from './service-tasks.stories.tsx';
import ServiceTasks from './tasks';

<Meta title={stories.default.title} component={stories.default.component} />

# Service tasks

<Story name="BasicServiceTasks" decorators={stories.default.decorators}>
	{stories.BasicServiceTasks()}
</Story>
