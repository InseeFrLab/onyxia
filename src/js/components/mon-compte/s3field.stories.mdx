import { Props } from '@storybook/addon-docs/blocks';
import * as stories from './s3field.stories.js';
import S3Field from 'js/components/mon-compte/s3.tsx';

<Meta title={stories.default.title} component={stories.default.component} />

# S3Field (change this name)

<Props of={S3Field} />

When your service is started, your card is like:

<Story name="BasicS3Field" decorators={stories.default.decorators}>{stories.BasicS3Field()}</Story>