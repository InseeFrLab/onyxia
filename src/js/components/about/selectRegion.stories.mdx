import { Props, Story } from '@storybook/addon-docs/blocks';
import * as stories from './selectRegion.stories.tsx';
import SelectRegion from './SelectRegion.tsx';

<Meta title={stories.default.title} component={stories.default.component} />

# Select region

This component lets the user choose a region.

<Props of={SelectRegion} />

If there are no regions available :

<Story name="NoRegions" decorators={stories.default.decorators}>
	{stories.NoRegions()}
</Story>

If there are regions but no location associated :

<Story name="WithoutLocation" decorators={stories.default.decorators}>
	{stories.WithoutLocation()}
</Story>

If there are regions and location :

<Story name="WithLocation" decorators={stories.default.decorators}>
	{stories.WithLocation()}
</Story>

If we handle region selection :

<Story name="WithEvent" decorators={stories.default.decorators}>
	{stories.WithEvent()}
</Story>
